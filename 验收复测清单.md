# 验收复测清单（填完把结果贴回对话即可）

> 用途：上一轮修复了三项问题，请按本清单复测并逐条勾选；把结果（含现象/报错截图路径）
> 发回给我即可继续收尾。详细步骤见 `README.md`。

## 前置：确认已用最新 HEX 与最新代码
- [ ] 重新烧录 `HEX\Wired_P1.hex` 到**板1**、`HEX\Wired_P2.hex` 到**板2**
      （统一 HEX 目录，命名见 `HEX\README.md`；含数码管显示修复，Keil 0 Error）
- [ ] 重启游戏：`cd PC && python main.py`（重开相关改动在 Python 代码中，无需重编译；板子需重烧含 bit5(K2) 的新 HEX）

## A. 三项修复复测
| # | 复测项 | 预期 | 结果(PASS/FAIL) |
|---|---|---|---|
| A1 | 板1 数码管与灯 | 数码管最左位显示 **1**，且 L0 亮 | ☐ |
| A2 | 板2 数码管与灯 | 数码管最左位显示 **2**，且 L7 亮 | ☐ |
| A3 | 重开（两种方式任选） | 打完一局（生命归零出现"获胜!"）后，**鼠标点击画面** 或 **任一手柄按 K2**，应恢复初始对局可继续玩 | ☐ |
| A4 | Esc 退出（对照） | 按 Esc 可退出（若 A3 两种方式均无效，运行 `python main.py --debug` 复现并把 `PC\debug_keys.log` 内容发回） | ☐ |

## B. 完整验收标准 1~7（真机）
| # | 验收项 | 操作 | 结果 |
|---|---|---|---|
| 1 | 身份区分 | 板1 显示"1"+L0；板2 显示"2"+L7（即 A1/A2） | ☐ |
| 2 | 移动 | 按导航键，PC 端对应坦克移动/转向 | ☐ |
| 3 | 开火 | 按 K1，坦克发射子弹（按住不连发，同屏≤3 发） | ☐ |
| 4 | 命中 | 击中对方 → 对方生命-1、自己 +10 分、对方回出生点短暂无敌 | ☐ |
| 5 | 胜负 | 任一方生命归零 → 中央显示"玩家X 获胜！" | ☐ |
| 6 | 障碍物 | 坦克不能穿过灰色障碍；子弹碰到障碍消失 | ☐ |
| 7 | 重开 | 对局结束后点击鼠标或任一手柄按 K2，恢复初始状态重新开始（即 A3） | ☐ |
| 附 | 断线 | 拔某板 USB → 顶部"断开!"橙色点；插回自动恢复 | ☐ |

## 汇报模板（直接复制填写发我）
```
修复复测：A1=__ A2=__ A3=__ A4=__
验收清单：1=__ 2=__ 3=__ 4=__ 5=__ 6=__ 7=__ 断线=__
未通过项现象描述：
（可附截图路径，如 PC/tests/shots/ 或自行截图）
```

> 提示：如果只是想知道某一步具体怎么操作（如 STC-ISP 频率选择、双板串口端口对应），
> 或复测中出现任何报错/异常现象，直接把信息贴给我即可。
