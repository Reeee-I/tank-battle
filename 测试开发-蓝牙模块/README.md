# 测试开发-蓝牙模块（第三种通信方式：蓝牙无线通信）

> **状态：测试开发中。** 已实测：BT05 的 **BLE 上行链路打通**（PC 稳定收到
> `AA 01 00` 心跳帧）；卡点 = "PC 侧把 BLE 字节流变成游戏可打开的 COM"，
> 因 Win11 25H2 拒绝加载 com0com 老签名驱动（577），三条出路已记录于
> `02_测试文档/BLE桥接方案.md`（推荐改走 HC-05/HC-06 经典蓝牙正式路线）。
> 测试通过后按 `03_文档/08_整理记录/整理说明.md` 的目录规则并入正式结构
> （另立整理任务，本文件夹届时归档）。
> 本次测试**不修改任何既有文件**（`01_下位机程序/`、`02_上位机程序/`、
> `03_文档/`、根 `HEX\` 均保持原样）。

## 目标

玩家操作的 STC-B 学习板通过**蓝牙透传模块**（UART TTL，9600 8N1）把按键操作
传到上位机。协议沿用既有 3 字节帧族（上行 `AA 01/02 掩码`、下行 `AA D1/D2 HP`），
**PC 端程序零改动**。两条硬件路线：

- **路线 A（正式）**：HC-05/HC-06 **经典蓝牙**模块 → Windows 蓝牙虚拟 COM
  → 游戏用 `--p1/--p2` 指定 COM（自动扫描跳过蓝牙口是既有设计）；
- **路线 B（本机实际硬件，测试绕过）**：模块实测为 **BT05（CC2541 BLE 透传）**，
  Windows 不会给它 COM → 用 `com0com` 虚拟串口对 + `ble_bridge.py` 把 BLE 字节流
  搬成普通 COM 给游戏（详见 `02_测试文档/BLE桥接方案.md`）。

实测结论：BT05 的 **BLE 上行链路已打通**——PC 经 bleak 订阅 0xFFE1 稳定收到
`AA 01 00`（4 秒 41 帧 ≈ 100ms/帧，与固件心跳一致）。

## 目录

```
测试开发-蓝牙模块/
├── README.md              ← 本文件
├── 01_蓝牙手柄固件/        # 板端固件 + Keil 工程（main.c 为 GBK 编码）
│   ├── main.c             # 蓝牙手柄固件（宏 PLAYER_ID=1/2 由工程 Define 区分）
│   ├── BT_Player1.uvproj  # 玩家1 → BT_Player1.hex
│   └── BT_Player2.uvproj  # 玩家2 → BT_Player2.hex
├── 02_测试文档/
│   ├── 接线与模块配置.md   # 接线 / HC-05·HC-06 配置 / 配对与 COM / FAQ
│   ├── 联调与验收步骤.md   # 阶段A裸链路→B单块进游戏→C下行LED→D双蓝牙 + 验收清单
│   └── BLE桥接方案.md      # ★ BT05(BLE) 路线：com0com + ble_bridge 接游戏
├── 03_测试工具/
│   ├── ble_bridge.py       # ★ BLE↔虚拟串口 双向桥接（bleak），自动重连
│   ├── fetch_bleak_deps.py # 换机时重新拉取 bleak+winrt 依赖到 _bleak_deps/
│   ├── bt_port_test.py     # 经典蓝牙 SPP 链路检测（路线 A 用）
│   ├── send_led_hp.py      # 独立下发 AA D1/D2 HP 帧验证下行 LED
│   ├── wait_link.py        # 等待游戏侧 COM 出现玩家帧（一键启动用）
│   ├── _bleak_deps/        # 本地依赖（bleak+winrt，未入库，换机用上面的脚本生成）
│   └── com0com_x/          # com0com 安装包（已弃用，保留勿删）
├── 启动蓝牙对战.bat        # ★ 双击即可：开桥接→等链路→启动游戏（日常使用入口）
└── HEX/                   # ★ 测试期 hex 单独放置（Keil Output 指向这里）
    ├── BT_Player1.hex     # 编译产物
    ├── BT_Player2.hex
    └── README.md
```

共享资源按既有约定**只引用不复制**：`01_下位机程序/common/inc`、`common/STC_BSP.lib`、
`01_下位机程序/04_音效模块/sound_effect.c`。

## 与正式版既有接法的关系

| 方案 | 板→PC 链路 | PC 数据口 | 下行 LED |
|---|---|---|---|
| ① 485 三板（主推） | 手柄→485→中转板→USB | 1 | ✅ 经中转板转发 |
| ② 有线 USB | 手柄 USB 直连 | 2 | ✅ |
| ③ 红外无线 | 手柄→红外→接收板→USB | 1 | ❌ 无下行 |
| **④ 蓝牙（本次测试）** | **手柄 UART2→蓝牙模块→PC 蓝牙** | **2（每板一个蓝牙 COM）** | **✅ 直达** |

## 快速开始（详见 02_测试文档）

```bat
:: 1) 编译：Keil 打开 01_蓝牙手柄固件 下 BT_Player1/2.uvproj → F7
::    产物在 HEX\BT_Player1.hex / BT_Player2.hex
:: 2) 烧录：STC-ISP，STC15F2K60S2，11.0592MHz，烧完关闭 STC-ISP
:: 3) 接线+配对（见 接线与模块配置.md），笔记本蓝牙各出一个 COM
:: 4) 裸链路自测
cd 02_上位机程序
python tools/serial_probe.py COMx
:: 5) 进游戏（示例：玩家2 蓝牙 + 玩家1 有线板）
python main.py --p1 COM3 --p2 COM6
```

## 测试结论（截至 2026-09-06 · 游戏实测通过）

| 项目 | 结论 |
|---|---|
| 编译 0 Error（BT_Player1/2.hex，Keil 实测） | ✅ 0 Error（BSP 库 L16 警告与既有工程一致） |
| 蓝牙模块实测型号 | ⚠️ **BT05 = CC2541 BLE 透传**（非 HC-05 经典蓝牙）→ 走 BLE 桥接路线 |
| BLE 上行（板→BT05→PC，心跳/按键 `AA 01`） | ✅ 实测：100ms/帧稳定（8s≈80 帧） |
| 下行 LED 血量（PC→BLE→板） | ✅ 实测：满血 6 灯 / 死亡全灭（VSPD 路线全双工） |
| **游戏实测（`--p1 COM20` 蓝牙手柄）** | ✅ **2026-09-06 用户确认成功**（VSPD 替代 com0com，解决 Win11 25H2 签名问题） |
| 日常使用流程 | 双击 `启动蓝牙对战.bat`（3 步封装），详见 BLE桥接方案.md 第六节 |
| 既有方案回归（无任何既有文件改动） | ✅ git 复核：仅测试文件夹新增，正式目录零改动 |
| 双蓝牙并行互不干扰 | 待测（需两块板+两模块；届时两块板各连一路 BLE 桥） |
