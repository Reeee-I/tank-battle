# 「双人坦克对战」项目开发计划（持续维护版）

> 本文件是项目的唯一计划源，随开发进展**持续维护**：每完成一个阶段、每做一次设计变更，都在文末"变更日志"追加记录并更新正文，保证文件内容始终等于"当前最新计划"。
>
> - 最近更新：2026-09-05（v2.11，蜂鸣器音效真机试听通过归档）
> - 当前状态：**阶段 1~6 全部完成；验收标准 1~7 用户真机确认全过（USB 直连旧方案）；v2.1 新增 6 张地图/每局随机/开局直线遮挡；v2.7 新增 485 三板手柄方案（PC 端零改动，三 HEX 编译 0 Error，全套自测回归绿，真机联调待执行）；v2.8 手柄 LED(L0~L5) 显示所控角色血量（满血6灯/掉血灭灯/死亡全灭），身份改由数码管承担，新增 PC→板下行帧 AA D1/D2 HP（USB 直连/485 两套支持，无线不做），五份固件 UV4 实测 0 Error，PC 全量回归绿（逻辑30/地图4/链路12/验收预演1~8/画面QA/文本QA）；v2.9 HEX 统一更名（485_Player1/2、485_Relay、USB_Player1/2、IR_Player2/IR_Relay），工程 OutputName/文档/HEX 目录同步，7 工程重编 0 Error 且更名前内容 MD5 不变（纯更名）；v2.10 用户真机复测通过（2026-09-04，USB 直连两板 与 485 三板两套接法，验收 1~8 含 LED 血量条全部确认）；v2.11 蜂鸣器音效测试开发（玩家手边板启用统一音效、电脑旁板禁用：新模块 sound\sound_effect 宏开关解耦、六份 HEX UV4 实测 0 Error，真机试听通过（2026-09-05 用户确认），详见 §11 与变更日志 v2.11）**

---

## 1. 项目目标与验收标准

使用两块 STC_B 学习板（STC15F2K60S2）作手柄 + PC（Python/Pygame）游戏程序，实现双人同屏坦克对战。板端通过串口每 100ms 上报 3 字节按键包，PC 端解析驱动游戏。

**验收标准（用户给定，必须逐条通过）**

1. 两块板子烧录后上电：**数码管最左位分别显示 1 / 2** 区分玩家身份
   （v2.8 用户需求变更：身份看数码管，**LED 改为显示所控角色血量**——
   上电默认全灭，不再亮 L0/L7）；
2. 按板子导航键，PC 端对应坦克移动；
3. 按板子 K1 键，PC 端坦克发射子弹；
4. 子弹击中对方坦克，对方生命 -1；
5. 任意一方生命归零，游戏显示获胜方；
6. 障碍物正常工作，坦克与子弹不能穿过；
7. 按 R 键重新开始游戏。（2026-09-02 用户最新需求变更：改为 **点击鼠标 或 任一手柄按 K2** 重开，R/回车/K1 不再用于重开；协议新增 bit5=K2 单发重开请求。）
8. **手柄 LED 血量联动**（v2.8 新增）：两块玩家手柄板 LED(L0~L5) 显示**本板所控角色**血量——
   满血(3)亮 6 灯；每掉 1 血灭右侧 2 灯；死亡全灭；血包回血补亮、K2/点击重开复位满血。
   （身份=数码管 1/2；上电/开始界面 LED 默认全灭。只在实际操作的玩家板上实现，
   中转/接收板不做；红外无线本期不做。）

**质量线（高要求，开发全程对照）**

- 界面美观：布局规整、配色协调、中文无乱码、生命/得分/状态显示清晰精致，无元素重叠或穿帮；
- 操作流畅：主循环稳定 60FPS，输入响应无卡顿、无阻塞、无抖动，串口断线有清晰提示且不崩溃；
- 健壮性：非法数据包不导致崩溃/乱动，拔线重插可自动恢复；
- 文档完整：README 按文档可复现"接线→烧录→运行→验收"全流程。

---

## 2. 现状核对结论（BSP 真实 API 与题目差异，开发以此为准）

| # | 题目描述 | 真实 BSP（inc 头文件为准） | 处理 |
|---|---|---|---|
| 1 | `GetAdcNavAct()` 无参、按住持续返回 | `GetAdcNavAct(char Nav_button)` 需传参；返回**边沿事件** `enumKeyPress/Release`，**查询一次即复位** | 自维护 `navHold` 状态位：100ms 轮询四方向，Press 置位 / Release 清位 → 实现按住持续 |
| 2 | 导航键名 | `enumAdcNavKeyUp/Down/Left/Right/Center`（含 `enumAdcNavKey3`） | 按真实枚举名传参 |
| 3 | `Uart2Print()` 发送 | 返回 `enumUart2TxOK/Failure`（请求是否被接收）；`GetUart2TxStatus()` 返回 `enumUart2TxFree/Busy` | 发送前查 TxFree；**开火位在 `Uart2Print` 返回 OK 后才清除**（保证单发不丢不重） |
| 4 | `LedPrint(0x01)/(0x80)` 区分玩家 | `bit=1 亮`：0x01→L0、0x80→L7 | 与题目一致，照做 |

其它已确认：`AdcInit(ADCexpEXT)`（=0x98，保留 P1.0/P1.1 作数字 IO，供 UART2-EXT 用）、`KeyInit()`/`GetKeyAct(enumKey1)`（一次性事件）、`SetEventCallBack(enumEventSys100mS, ...)`、`MySTC_Init()` + `while(1){ MySTC_OS(); }`（BSP 铁律，循环内不得出现其它代码）。

**环境事实**：工作目录含 BSP 头文件（`MCU\inc\`）与两份 API 文档；`STC_BSP.lib` 已放于 `MCU\`（485 三工程共享引用）；**本机已装 Keil C51/UV4（D:\Keil），板端代码可用 UV4 -b 命令行实编译验证**（0 Error，警告仅 BSP 库 L16）；STC-ISP 下载频率须与 `SysClock`（11.0592MHz）一致。

---

## 3. 总体架构

主推接法（v2.7 起）：**两块手柄板走 RS-485 共享总线，第三块中转板轮询收包并经
USB(串口1) 原样转发 PC**——PC 端协议/代码零改动，两玩家从同一个 COM 口按包内容
自动绑定；旧 USB 直连两板（两个 COM）与红外无线方案保留可选，PC 三种接法通用。

```
[手柄1板]──┐                              ┌─ UART2/485 轮询主机 ─┐
[手柄2板]──┼── RS-485 共享总线(A/B/GND)──→│  中转板(第三块板)      │── USB/串口1 ──→ PC
 每100ms采样  9600bps 8N1 半双工          │  点名P1→收→点名P2→收  │  9600bps 8N1
 被点名才应答                             └── 校验后原样转发 ──────┘  (1个COM口)
```

- 板端固件分三族（PC 端协议一致、通用）：① 485 三板手柄（`485手柄\`，v2.7 主推）；
  ② 旧 USB 直连（`MCU\`，可选）；③ 红外无线（`无线手柄\`，可选）。
- 玩家身份恒由 3 字节包内容（ID 0x01/0x02）识别：旧方案"哪个 COM 口先收到玩家 X
  的合法包即绑定"，485 方案为同 COM 口内按包内容各自绑定，杜绝插错线问题。
- 身份显示：数码管最左位 手柄1='1'、手柄2='2'、485 中转板='C'、无线发送端='A'、
  无线接收端='b'；**LED 与身份解耦（v2.8）**——两块玩家手柄板 LED(L0~L5) 专作
  所控角色血条（满血6灯…死亡全灭），上电/未进对局全灭，由 PC 下行帧驱动。

## 4. 通信协议（定稿）

3 字节数据包，9600 bps、8 数据位、1 停止位、无校验：

| 字节 | 内容 | 取值 |
|---|---|---|
| 0 | 包头 | 固定 `0xAA` |
| 1 | 玩家 ID | `0x01`（玩家1）/ `0x02`（玩家2） |
| 2 | 按键位掩码 | bit0 左 / bit1 右 / bit2 上 / bit3 下（按住持续）；bit4 开火 K1（**单发**）；bit5 重开请求 K2（**单发**，PC 仅在结算画面处理）；bit6 K3（**单发**，PC 开始界面"任意键开始"用，对战中忽略） |

例：玩家1按住"上"并开火 → `AA 01 14`（0x14=00010100）。

### 4.1 PC → 板 下行帧（LED 血量联动，v2.8 新增）

同波特率、3 字节（命令码与玩家号 01/02、点名码 E1/E2 永不冲突）：

| 字节 | 内容 | 取值 |
|---|---|---|
| 0 | 包头 | 固定 `0xAA` |
| 1 | 下行命令码 | `0xD1`（玩家1 血量）/ `0xD2`（玩家2 血量） |
| 2 | 血量 | `0~3`（3=满血） |

- LED 血条语义（只作用于两块玩家手柄板）：满血3=**L0~L5 六灯**、每掉 1 血灭
  **右侧 2 灯**（2→4 灯、1→2 灯）、死亡(0)=**全灭**；身份=数码管 1/2；
  上电/未进对局全灭。
- 发送时机：开局（满血3）、每次命中扣血、血包回血、死亡归零、K2/鼠标重开复位；
  板子重插/重连自动补发一次当前血量（串口层按"打开代数"触发）。
- 发送路径：USB 直连 = PC 写各玩家绑定 COM；485 = PC 写中转板 COM，中转板在
  总线**空闲窗口**（点名/应答结束 ≥20ms，每 10ms 至多一帧）转发给对应手柄；
  红外无线方案本期不支持。

---

## 5. 交付目录结构（本工作目录 `A大项目` 下）

```
A大项目/
├─ README.md                  # 总说明：接线、运行、烧录、验收清单、FAQ
├─ 开发计划.md                # 本文件（持续维护）
├─ HEX/                       # ★ 统一 HEX 输出目录（所有工程 Output 指向，命名见 HEX\README.md）
│  ├─ 485_Player1.hex       # 485 手柄1（数码管 '1'）v2.7 新增
│  ├─ 485_Player2.hex       # 485 手柄2（数码管 '2'）v2.7 新增
│  ├─ 485_Relay.hex           # 485 中转板（数码管 'C'）v2.7 新增
│  ├─ USB_Player1.hex / USB_Player2.hex          # 旧 USB 直连玩家1/2（原 Tank_P*.hex）
│  ├─ IR_Player2.hex / IR_Relay.hex # 无线发送端/接收端
│  └─ README.md               # HEX 命名/用途对照表
├─ 485手柄/                   # 485 三板手柄方案（v2.7 主推，详见其 README）
│  ├─ main_handle.c           # 手柄源（两块共用，PLAYER_ID 宏区分 1/2）
│  ├─ main_hub.c              # 中转板源（485 轮询主机 + USB 转发）
│  ├─ RS485_Handle1.uvproj / RS485_Handle2.uvproj / RS485_Hub.uvproj
│  └─ README.md               # 485 接线/轮询协议/烧录/联调/FAQ
├─ MCU/                       # 旧 USB 直连方案板端（仍可选；v2.7 起不作为主推）
│  ├─ main.c                  # 唯一源文件（C89，两板通用，宏区分玩家）
│  ├─ inc/  STC_BSP.lib       # BSP 头文件与库（485 三工程共享引用）
│  ├─ TankBattle.uvproj       # 双 Target：Player1/Player2
│  ├─ TankBattle_P1.uvproj / TankBattle_P2.uvproj  # 单 Target 便捷工程
│  └─ list/                   # Keil 列表目录
├─ sound/                     # 蜂鸣器音效模块与音效版 main（v2.11 测试开发；新增代码集中于此与各工程 sound_effect.c/h）
│  ├─ sound_effect.h/.c       # 统一音效模块：Sound_Init/Play/IsBusy/Tick；宏 SOUND_ENABLED/VOLUME/PRIORITY
│  ├─ main_wired_handle_sound.c / main_wireless_tx_sound.c / main_wireless_rx_disabled.c / main_485_handle_sound.c
│  │                          # 各板"音效测试版"main 副本（改动点带【音效】标注，已同步覆盖各工程 main.c）
│  ├─ README.md               # 交付物对照 / 快速集成
│  └─ 音效测试与集成说明.md    # 音量控制 / 集成步骤 / 测试步骤与预期 / FAQ / 验证记录
└─ PC/                        # PC 端（协议/代码 v2.7 零改动）
   ├─ main.py  game.py  tank.py  bullet.py  obstacle.py  serial_handler.py  powerup.py
   ├─ requirements.txt        # pygame, pyserial
   ├─ tools/serial_probe.py   # 调试工具：裸串口监听解码（验收/排障）
   └─ tests/                  # 自测：test_logic(28项) + test_maps(4项) + test_serial_e2e(8项，含485中转单COM)
                              # + acceptance_sim(验收1~7) + visual_qa(截图) + text_layout_qa
```

> v2.11 起：`sound_effect.h/.c` 已复制进 `MCU\`、`485手柄\`、`无线手柄\HEX_A_IR_Tx\` 并挂入对应 Keil 工程（接收端 HEX_B 只放头文件、不挂 .c；485 中转板不挂）； 无线发送端工程 `inc\` 补入 `Beep.h`；各工程 main.c 已换成音效测试版。详见 §11。

## 6. 板端设计（MCU/main.c，旧 USB 直连方案固件）

> 注：本节约对应**旧 USB 直连方案**（`MCU\`，仍可选）；v2.7 主推的 485 三板方案
> 手柄/中转板固件设计见 §8.1 与 `485手柄\main_handle.c`、`485手柄\main_hub.c`
> （同源逻辑：100ms 采样保持一致，仅发送链路与触发方式不同）。

### 6.1 已确认决策
- **通信链路（用户定）**：默认"USB 线直连"（板载 USB = 串口1，CH340→P3.0/P3.1）；同时用编译宏 `UART_IF` 保留 UART2-EXT 备选方案。发送统一封装 `uart_send()`/`uart_tx_free()`，`#if` 切换，业务代码共用。
  - `UART_IF=0`（默认）：`Uart1Init(9600UL)` + `Uart1Print`/`GetUart1TxStatus`/`enumUart1TxFree`；
  - `UART_IF=1`：`Uart2Init(9600UL, Uart2UsedforEXT)` + `Uart2Print`/`GetUart2TxStatus`/`enumUart2TxFree`。
- **玩家区分**：`PLAYER_ID` 宏（1/2）；Keil 工程双 Target（P1/P2）各自 Define `PLAYER_ID=1/2`，出两个 HEX；也可手动改宏重编。

### 6.2 初始化顺序
```c
DisplayerInit();
LedPrint(0x00);                             // v2.8：LED=血量条，上电全灭（身份看数码管）
Seg7Print((PLAYER_ID==1)?1:2, 10,10,10,10,10,10,10);  // 数码管身份 1/2
KeyInit();
AdcInit(ADCexpEXT);                         // 保留 P1.0/P1.1
UartXInit(9600UL, ...);                     // 按 UART_IF 选串口1或2
#if (UART_IF == 0)
SetUart1Rxd(dlRxBuf, 3, dlHead, 2);         // 接收 PC 下行血量帧（AA+本板命令码）
#endif
SetEventCallBack(enumEventSys100mS, my100mS_callback);
#if (UART_IF == 0)
SetEventCallBack(enumEventUart1Rxd, myUart1Rxd_callback);  // 下行血量事件
#endif
MySTC_Init();
while(1){ MySTC_OS(); }                     // BSP 铁律
```

### 6.3 100ms 回调组包算法
```c
static unsigned char navHold;      /* bit0左/1右/2上/3下：按住状态位 */
static unsigned char firePending;  /* 开火待发标志（单发） */
static unsigned char txBuf[3];

void my100mS_callback(void) {
  unsigned char act, i, byteOut;

  /* ① 轮询4个导航方向：Press置位/Release清位 → 实现按住持续 */
  for (i=0;i<4;i++){
    act = GetAdcNavAct(对应方向枚举);
    if (act==enumKeyPress)            navHold |=  (1<<i);
    else if (act==enumKeyRelease)     navHold &= ~(1<<i);
  }
  /* ② K1开火：仅取按下瞬间 */
  if (GetKeyAct(enumKey1)==enumKeyPress) firePending=1;
  /* ③ 组包 */
  byteOut = navHold;
  if (firePending) byteOut |= 0x10;
  txBuf[0]=0xAA; txBuf[1]=(PLAYER_ID==1)?0x01:0x02; txBuf[2]=byteOut;
  /* ④ 发送：空闲才发；成功入队后才清开火标志 → 单发不丢不重 */
  if (uart_tx_free()) {
     if (uart_send(txBuf,3)) firePending=0;
  }
}
```
要点：9600bps 下 3 字节 ≈ 3.1ms ≪ 100ms 发包间隔；回调总耗时 ≪1ms，不违反系统调度限制；C89 规范（变量前置声明）。

### 6.4 Keil 工程配置（手动步骤为主，.uvproj 为参考）
1. STC-ISP「Keil仿真设置」安装 STC 设备库与头文件到 Keil；
2. 新建工程：芯片选 **STC15F2K60S2**；
3. 工程文件：`main.c` + `STC_BSP.lib`；Include Paths 指向 `MCU\inc`（含 sys.H、displayer.h、uart1.h/uart2.h、Key.H、adc.h、STC15F2K60S2.H）；
4. Target Options：内存模式 **Small**、代码优化 **Level 8**、勾选 **生成 HEX**；
5. 双 Target（P1/P2）：C51 选项 Define 分别设 `PLAYER_ID=1` / `PLAYER_ID=2`；分别编译得 `Tank_P1.hex`、`Tank_P2.hex`。

### 6.5 LED 血量联动（v2.8 新增，板端/PC 分工）
- **下行帧**：PC 经串口下发 `AA D1/D2 HP`（HP=0~3；D1=玩家1、D2=玩家2，与玩家号/点名码不冲突）。
- **板端（本文件 MCU/main.c，UART_IF=0）**：`SetUart1Rxd(dlRxBuf,3,dlHead,2)` 只接收
  "AA+本板命令码"的帧 → `myUart1Rxd_callback` 夹取血量 → `LedPrint(hpLedTable[hp])`；
  `hpLedTable = {0x00,0x03,0x0F,0x3F}`（L0~L5 血条：0/1/2/3 命 → 0/2/4/6 灯）。
  上电 `LedPrint(0x00)` 全灭（LED 不再亮 L0/L7 身份灯，身份看数码管）。
- **PC 端**：`SerialHub.set_hp(pid,hp)` 组帧并写入该玩家绑定口；`SerialLink` 维护下行
  队列（接收线程内顺带写串口，避免跨线程并发读写）；Game 在 开局/命中/回血/死亡/重开
  调用 `_push_hp`；端口重连（打开代数+1）后收到首个上行包自动补发一次当前血量。
- 485 方案的对应实现见 §8.1（中转板空闲窗口转发 + 手柄端分发）。

---

## 7. PC 端设计（7 文件 + OOP）

### 7.1 serial_handler.py
- `SerialHub` 管理两个 `SerialLink`（各 1 条接收线程）；端口扫描用 `serial.tools.list_ports`，排除非串口；打开失败给出中文提示（如 STC-ISP 占用）。
- 字节流状态机：搜 `0xAA` → 校验 ID∈{0x01,0x02} → 读按键字节 → 更新 `(player, mask, last_seen)`；非法字节滑动窗口重同步。
- **自动绑定**：首个合法包的 ID 决定 COM口↔玩家；命令行 `--p1 COM3 --p2 COM5` 可手动指定。
- **断线处理**：某玩家 >600ms 无包 → 状态栏"玩家X 连接断开"，冻结其输入不清生命；每 2s 自动重开端口，重连恢复。
- `tools/serial_probe.py` 复用其做裸监听解码。

### 7.2 tank.py / bullet.py / obstacle.py / powerup.py
- 坦克 30×30 AABB、子弹 r=5 圆形、障碍 AABB；逐轴移动+碰撞回退（贴墙滑动）；子弹从炮口偏移生成。
- 由按键位掩码驱动动作（与板子协议解耦）；道具 PowerUp 为纯几何/逻辑（类型/位置/拾取半径）。
- v2.3 起：tank 带 speed/cannon/shield 三增益时间戳 + `clear_buffs()`；bullet 携带开火快照伤害；powerup.py 提供四种道具类型常量。

### 7.3 game.py（已确认规则）
- 每帧从 SerialHub 取两玩家最新掩码；无新包沿用上一帧 → 输入 10Hz、渲染 60fps 平滑。
- 常量集中：移动 3px/帧；子弹 5px/帧；每坦克最多 3 发；冷却 200ms；被击中回出生点 + 无敌 1s（闪烁表现）；生命归零判负。
- **得分（用户定）**：每命中对方一次 **+10 分**。
- **转向（用户定）**：`ROTATE_SPEED = 2.0`（°/帧，≈120°/s，集中可调，注释说明如何改回题面 5°/指令）。
- 出生：P1(150,300) 朝右(0°)，P2(650,300) 朝左(180°)；800×600。
- 地图（v2.1 起）：内置 **6 张镜像对称地图**（经典战场/十字要塞/中央堡垒/回廊/街巷/石柱阵），
  `main.py` 每局开局**随机抽取 1 张**（结算重开也会重抽；`Game(map_id=...)` 可固定某图复现/测试）。
  每张地图中央都设有掩体 → **两出生点正前方直线弹道被遮挡，开局无法隔空直射**（修复旧版
  "开局即对射"的不平衡）；对称性/出生留空/全图连通等约束由 `tests/test_maps.py` 自动校验；
  画面左下角显示当前地图名。
- 渲染：左上/右上 生命（代码自绘红心，避开 emoji 字体坑）+ 得分；顶部中央状态；右下实时 FPS（`clock.get_fps()`）；中文 `pygame.font.SysFont`（微软雅黑/黑体回退）。
- 重开=结算画面 点击鼠标 / 任一手柄按 K2、Esc 退出；**键盘双人调试模式**（P1: WASD+空格；P2: 方向键+回车），无板也可验收。
- 道具（v2.3 起，见下方"道具与平衡"设计与变更日志）：`powerup.py` 新增 PowerUp 类；
  `game.py` 道具常量区（节奏/数值/倍率全部集中可调）+ `_update_powerups`（到期清理→
  碾过拾取→定时生成）；`tank.py` 增加 speed/cannon/shield 三增益时间戳与 `clear_buffs()`；
  `bullet.py` 子弹携带开火瞬间快照的伤害；HUD 增益栏（加速/炮强/护盾 + 剩余秒）在
  生命/得分行下方；四种道具发光图标 + 增强弹大光环 + 青色护盾环渲染。
  **道具与平衡设计**：
  - 节奏（v2.5 微调）：首件开局 6s，之后每 10~15s 随机 1 件、四类等概率；场上同时 ≤2 件；每件
    10s 无人拾取消失（临消失 3s 闪烁）；刷点要求"整辆坦克可停靠"（避障碍/坦克/道具）；
  - 数值：加速 6s×1.5；炮弹增强 6s、命中 -2 血、伤害在开火瞬间快照（视觉为白色大光环弹）；
    血包 +1 命（上限 3），满血拾取无效并消耗；护盾 8s 或挡 1 发（先到先失效）；
  - 防滚雪球：阵亡重生清空全部增益与护盾；护盾挡击不掉血/不重生/不清其他增益；
    血包天然偏向落后方（领先方满血拿了也是白拿）→ 反滚雪球；增强炮短时 + 可被反制；
  - 不叠加：同类重复拾取刷新满时长。
  - 全套回归：`test_logic.py` 新增 8 项道具测试（24 项总）全部通过。
- v2.4 起（交互细节）：HUD 增益栏**实时倒数**（`_buff_seconds` 静态方法，修复原"恒显 0s"
  ——剩余时长被二次减 now 的 bug）；血包拾取头顶**浮动提示**（`toasts`：回血绿"生命 +1"、
  满血灰"生命已满"，1.2s 上浮渐隐）；**开始界面**（`Game(menu=True)`，键盘任意键/鼠标点击/
  任一手柄任意键进入对战，期间冻结；结算 K2/点击重开直接开下一局不经过开始界面）；
  回归：test_logic 28 项总、visual_qa 增 s09/s10、text_layout_qa 增 menu 审计。

### 7.4 main.py
打印欢迎信息 → 串口扫描/自动绑定 → 创建 Game 主循环；无串口警告并进入键盘调试模式。

---

## 8. 485 板间轮询链路与硬件接线（v2.7 主推）

### 8.1 485 板间轮询链路（板间私有，对 PC 透明）
- 总线：三块板 485 接口 A/A、B/B、GND 并联；9600bps 8N1 半双工；所有帧固定 3 字节，
  保证总线字节流按帧对齐（点名回声/应答互不干扰，靠包头与点名码过滤）；
- 点名帧（中转→手柄）：`AA E1 00`（玩家1）/ `AA E2 00`（玩家2）——E1/E2 为点名码，
  与玩家号 0x01/0x02 永不冲突；应答帧（手柄→中转）：`AA 01 mask` / `AA 02 mask`，
  与 PC 协议完全一致，中转校验后**原样转发**；
- 时序（中转板每 100ms 一个周期，10ms 事件做超时计数，全非阻塞）：点名 P1 → 收答即
  转发并点名 P2（P1 30ms 无答也点名 P2）→ 收 P2 答转发，周期结束；单手柄掉线不阻塞
  另一玩家，缺包与否由 PC 侧 0.6s 心跳超时判定（橙点"断开!"→自动恢复）；
- 手柄端：100ms 回调只采样（导航按住位 + K1/K2/K3 单发标志），UART2(485) 收包按
  帧第 2 字节分发：被点名码（E1/E2）点名才组包应答（应答被接受后清单发标志 →
  单发不丢不重）；收到本板血量命令码（D1/D2）只刷新 LED 血条、不应答；其它帧过滤；
- **下行血量帧（v2.8）**：PC 经串口1 下发 `AA D1/D2 HP` → 中转板 `myUart1Rxd_callback`
  缓存（`downP1/downP2` + 待发标志）→ 在 485 总线**空闲窗口**（waitP1/waitP2 均 0
  且 tickCnt≥`DL_SEND_TICKS(2)`，每 10ms 至多一帧）经 UART2 原样转发；手柄端按码
  匹配后刷新 LED(L0~L5) 血条（满血6灯/掉血灭灯/死亡全灭，上电全灭，身份看数码管）；
- 固件/工程/HEX：`485手柄\main_handle.c`（两工程共用）+ `485手柄\main_hub.c`
  （中转板）；三个工程 `RS485_Handle1.uvproj` / `RS485_Handle2.uvproj` /
  `RS485_Hub.uvproj` 共享引用 `MCU\inc` 与 `MCU\STC_BSP.lib`，
  Output 指向 `HEX\` → `485_Player1.hex` / `485_Player2.hex` / `485_Relay.hex`；
  详细接线/烧录/联调见 `485手柄\README.md` 与 `HEX\README.md`。

### 8.2 硬件接线（三种方案，485 为当前主推）

| 方案 | 连线 | 说明 |
|---|---|---|
| **A（主）485 三板** | 两手柄板 485 A/A、B/B、GND 并成一条总线接中转板；中转板 USB 接 PC；手柄板 USB 仅供电（充电头/充电宝/PC 其它口均可） | 一根 USB = 一个 COM；协议见 8.1；总线两端 120Ω 终端电阻视板面预留（桌面短距可先不加） |
| **B（旧方案，可选）板载 USB** | 每块板一根 USB 线直连 PC（板载 CH340→P3.0/P3.1） | 两个 COM；旧 USB 直连方案（`MCU\`） |
| **C（备）UART2-EXT** | 每板 EXT 口 P1.1(TXD2)→USB-TTL 模块 RX、GND 共地（P1.0/RX 可不接，纯上行） | 需 2 个 CH340；仅 MCU 旧工程支持（`UART_IF=1` 重编） |

## 9. 开发阶段与验收（每阶段完成即回填状态）

| 阶段 | 内容 | 验收点 | 状态 |
|---|---|---|---|
| 1 | 建目录、MCU/inc 拷贝、requirements、常量定义 | 目录与 §5 一致 | ✅ 完成 |
| 2 | 板端 main.c 完整实现 | 对照 §6.3 逐条评审；C89 自查 | ✅ 完成（无 `//` 注释、变量前置、宏齐全） |
| 2b | Keil .uvproj 双 Target 模板 | 双 Target/HEX/Define 校验 | ✅ 完成（XML 解析通过；基于用户旧工程模板改造） |
| 3 | PC：serial_handler → tank/bullet/obstacle → game → main | 键盘模式跑通验收 2~6 | ✅ 完成 |
| 4 | PC 自测 | 逻辑/链路/渲染验证 | ✅ 完成（见下方自证记录） |
| 5 | 文档：README（接线/烧录/运行/验收/FAQ） | 按文档可完整复现 | ✅ 完成 |
| 6 | 真板联调：绑定→移动→开火→击中→断线重连 | 对照 §1 验收 1~7 逐条打勾 | ✅ 完成：用户真机验收 1~7 全过（2026-09-02"全过"）；期间按反馈迭代：数码管身份显示、P2 编译路径、重开方式收敛为 鼠标点击/任一手柄K2（协议 bit5） |
| 7 | 蜂鸣器音效测试开发（v2.11，sound\ 集中存放；音效版 main 已覆盖各板工程） | 模块单独编（ENABLED=1/0）0 Error 0 Warning；六工程 UV4 0 Error（警告仅 BSP 库 L16）；宏关闭行为与原版一致、原功能不回归；真机试听按 §11.5 /《音效测试与集成说明.md》执行 | ✅ 完成：开发/编译/文档 + 用户真机试听通过（2026-09-05） |

### 自测证据（2026-09-02，本机执行）
- `PC/tests/test_logic.py`：16/16 PASS（协议解析/移动/转向/边界/障碍/坦克互撞/开火冷却/3发上限/子弹越界回收/命中扣血加分重生/无敌吸收/KO清场防重复结算与幽灵弹/胜负判定与重开reset/结算画面K2重开(K1不触发)/结算画面鼠标点击重开/断线心跳）；
- `PC/tests/test_serial_e2e.py`：7/7 PASS（socketpair 虚拟串口：发包→自动绑定→噪声重同步→超时离线→恢复在线→虚拟板直接驱动 Game 前进/开火，覆盖"板→PC→游戏"全链路）；
- `PC/tests/acceptance_sim.py`：验收标准 1~7 自动化预演全部通过（身份绑定/导航驱动/障碍阻挡/开火命中/扣血得分/胜负判定/手柄K2重开；验收1 的 L0/L7 亮灯由用户真机确认）；
- 独立交叉审查（两轮，PC 端 + 板端 C 各一）：PC 端发现并修复「KO 后同帧残弹重复结算/幽灵子弹」真 Bug、补"坦克互撞不穿模"、重连解析器残留、无 pyserial 崩溃路径、pygame≥2 校验；板端按审查采纳 PLAYER_ID 编译期 #error 校验、Uart1/2Print 成功判据改用 !=Failure（降低对库返回值的过度假设）。修复后全部回归通过；
- 逻辑性能基准：高压场景（双车移动/转向/周期开火）12000 帧平均 **3.66µs/帧**，仅为 60FPS 帧预算(16667µs)的 0.02%，流畅度余量充足；
- 无头渲染像素核查：开局/激战/胜负 三帧 10/10 PASS（坦克/障碍/红心/子弹/状态栏/胜利文字均按预期出现，无缺漏）；
- 画面 QA（`PC/tests/visual_qa.py`，收编为交付物）：六状态 + 重开语义（K1 不触发/K2 触发/鼠标点击触发）共 12 项断言 PASS，截图存 `PC/tests/shots/` 供人工预览；
- 文本 QA（`PC/tests/text_layout_qa.py`）：HUD 全量文本（含"COM10 断开!"长串场景）无越界、无重叠；34 个常用汉字字形内部着墨密度正常（无豆腐块/乱码）——中文渲染质量程序化可证；
- `python main.py` 冒烟：无串口环境正常启动进入 60FPS 主循环，无异常；
- MCU 侧：`TankBattle.uvproj` 双 Target（Player1→Tank_P1.hex/PLAYER_ID=1、Player2→Tank_P2.hex/PLAYER_ID=2，Optimize=8、Small、HEX 开启、大小写不敏感符号保持关闭）XML 校验通过；另生成单 Target 便捷工程 `TankBattle_P1.uvproj` / `TankBattle_P2.uvproj`（XML 校验通过）；
- **Keil 实编译确认（2026-09-02，本机 D:\Keil\UV4 命令行 UV4 -b 实测，含 K2 改动后再次验证）**：Player1/Player2 双 Target 均 **0 Error(s), 8 Warning(s)**（警告均为 BSP 库内未调用段 L16，无害）；`MCU\output\Tank_P1.hex` 与 `Tank_P2.hex` 均生成且 MD5 不同（身份/重开代码差异正确）——"实编译确认"关口已关闭；
- **用户真机验收（2026-09-02）：验收标准 1~7 全部确认通过（"全过"）**——L0/L7+数码管身份、移动转向、K1 开火、命中结算、胜负显示、障碍物、鼠标点击/K2 重开均真机验证通过；两轮评审+全部自动化自测+Keil 实编译 0 Error 证据齐备，项目交付完成。
- 真机首测反馈（2026-09-02，用户执行）：Tank_P1.hex 写入正常、可作手柄驱动游戏、画面美观 → 按反馈完成三项修复：① `main.c` 数码管最左位显示手柄号 1/2（替代默认 00000000，需重编两块板）；② `game.py` R 键重开三重容错（KEYDOWN + unicode + 原始键态检测，防输入法吞键），visual_qa 新增 s07 断言并 PASS；③ 补齐 Tank_P2.hex 编译指引 + 单 Target 便捷工程；④ 本机 UV4 命令行实测编译通过并产出两个最新 HEX。

## 10. 风险与对策
1. Keil（D:\Keil\UV4）命令行可实编译（UV4 -b，五工程 0 Error）；`.uvproj` 依赖 STC-ISP 安装的 Keil 设备库，换机需先装 STC 设备库再编译；
2. 485 半双工总线上插入下行帧：已有意避开点名/应答窗口（空闲 ≥20ms 才发、每 10ms 至多一帧），最坏情形（PC 恰在点名瞬间发血量）也只需等下一周期补发，无碰撞风险；
3. COM 占用/插拔 → 内置提示与自动重连；数据校验失败 → 滑动窗口重同步；下行血量帧幂等、重连自动补发；
4. 中文/emoji 渲染 → SysFont 回退 + 自绘红心；
5. LED 血量联动为硬件观感项 → 下行帧已自动化验证（字节级），LED 亮灭已由用户真机复测确认（2026-09-04，验收 8 两套接法全过）。

---

## 11. 蜂鸣器音效（玩家手边板，v2.11 测试开发）

> 目标：为【玩家手边的所有板子】加"短促有力、频率分明、同一时刻只播一个音效、
> 高优先级可抢占"的蜂鸣器反馈；电脑旁的板（无线接收端 / 485 中转板）不发声。
> 本次为测试开发，**不影响任何已有功能**：新增代码集中在 `sound\`，宏
> `SOUND_ENABLED=0` 时所有音效函数编译为空操作，行为与原版完全一致；音效版
> main 已覆盖各板工程，改动点全部带【音效】标注。

### 11.1 板子角色与启用范围

| 板子角色 | 位置 | 音效 | 工程 main（v2.11 已覆盖） | SOUND_ENABLED |
| :--- | :--- | :--- | :--- | :--- |
| 玩家1/2 有线手柄（USB 直连 PC） | 玩家手边 | 启用 | `MCU\main.c`（PLAYER_ID=1/2） | 1 |
| 无线发送端 HEX-A（红外手柄） | 玩家手边 | 启用 | `无线手柄\HEX_A_IR_Tx\main.c` | 1 |
| 无线接收端 HEX-B（红外接收+串口转发） | 电脑旁 | 禁用 | `无线手柄\HEX_B_IR_Rx\main.c` | 0（不挂模块 .c） |
| 485 手柄1/2（可选，三板方案） | 玩家手边 | 启用 | `485手柄\main_handle.c`（PLAYER_ID=1/2） | 1 |
| 485 中转板 | 电脑旁 | 不加 | 未改动（main_hub.c） | — |

### 11.2 音效定义（所有玩家手边板一致）

| 事件 | ID | 频率 | 时长 | 优先级 | 听感 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 开火 | SOUND_FIRE | 1500Hz | 50ms | 高 | 短促"哒" |
| 移动 | SOUND_MOVE | 800Hz | 20ms | 低 | 方向按住每 100ms 一声"嗒" |
| 被击中 | SOUND_DAMAGE | 500Hz | 200ms | 中 | 低鸣"呜" |
| 击中敌人 | SOUND_HIT | 1200Hz | 80ms×2 | 中 | 双短促（预留待 PC 指令） |
| 游戏结束 | SOUND_GAMEOVER | 1500→500Hz | 各 100ms | 高 | 下扫"哔—嗡" |

### 11.3 模块设计与关键决策

- 对外 API：`Sound_Init()` / `Sound_Play(id)` / `Sound_IsBusy()` / `Sound_Tick()`；
  宏 `SOUND_ENABLED`（0=空操作）、`SOUND_VOLUME`（1~10，默认 3）、
  `SOUND_PRIORITY_ENABLE`（默认 1）均可在工程 C51 Define 覆盖；
- 状态机：main 注册 10ms 事件回调驱动 `Sound_Tick()`；音步序列（单音 / 双短促 / 下扫），
  音步间 10ms 静音；同一时刻只播一个音效——新音优先级 ≥ 当前则抢占（丢弃当前音剩余音步、
  蜂鸣器空闲瞬间接播），优先级低则丢弃；发声看门狗防状态机卡死；
- 音量：BSP `SetBeep` 只有"频率+时长"两参数、内部 PWM 占空比固定、库无占空比接口，约束又
  禁止直接操作寄存器 → 采用**硬件串联 100Ω~1kΩ 限流电阻**（`SOUND_VOLUME=3` ↔ 470Ω~1kΩ
  起步，20cm 清晰可听不刺耳），模块内统一发声入口 `Sound_Play`；
- 打断边界（BSP 限制）：发声期间再次 `SetBeep` 返回 Fail、库无 StopBeep → **正在响的单音
  无法 API 硬截停**，抢占实现为"丢弃旧音剩余音步 + 空闲瞬间立即接播"，听感足够利落；
- 触发源：K1 按下瞬间 → FIRE；方向键按住（navHold≠0）每 100ms → MOVE；复用 PC 下行血量帧
  `AA D1/D2 HP`（v2.8 已有通道）——血量下降沿 → DAMAGE、血量归零 → GAMEOVER（首帧只建基线、
  回血/重发同血量不发声，天然防重复）；SOUND_HIT 预留，待 PC 协议扩展"命中敌人"指令后触发。

### 11.4 交付物与工程改动

- `sound\`：`sound_effect.h/.c` 模块源码、四份音效版 main 副本、`README.md`、
  《音效测试与集成说明.md》（音量控制 / 集成 / 测试步骤与预期 / FAQ / 验证记录）；
- 工程同步：`MCU\`、`485手柄\`、`无线手柄\HEX_A_IR_Tx\` 复制并挂载 `sound_effect.c/.h`，
  HEX-A 的 `inc\` 补入 `Beep.h`；`无线手柄\HEX_B_IR_Rx\` 只放头文件（工程不挂 .c）；
  各工程 main 为音效测试版；485 中转板与 HEX-B 行为不变（不在电脑旁板加任何音效代码）。

### 11.5 验证与状态

- 编译（本机 Keil UV4 -b，2026-09-05）：USB_Player1/2、485_Player1/2、IR_Player2（音效启用）
  与 IR_Relay（接收端）六份 **0 Error**，警告仅 BSP 库 L16；`sound_effect.c` 单独编译
  （ENABLED=1 / =0）均 **0 Error 0 Warning**（禁用态无需 Beep.h）；
- `HEX\` 已就绪：玩家手边板 hex 均为音效版；IR_Relay 二进制与改前一致（行为未变），
  485_Relay 未重编（中转板不加音效）；
- 真机试听（✅ 2026-09-05 用户真机确认通过）：K1"哒"、方向"嗒嗒"（100ms 一次）、20cm
  清晰不刺耳、被命中"呜"、血量归零下扫音、接收端全程无声、原按键采集 / 串口(485/红外)
  / 显示功能无回归，全部符合预期。

---
## 12. 变更日志
| 版本 | 日期 | 变更内容 |
|---|---|---|
| v1.0 | 2026-09-02 | 初稿：需求梳理、BSP API 差异核对、板端/PC 端设计、六阶段计划 |
| v1.1 | 2026-09-02 | 采纳用户四项决策：①默认板载 USB 串口1（宏切换保留 UART2-EXT）；②Keil 手动步骤为主 + .uvproj 参考模板；③得分=每命中 +10；④转向 2°/帧≈120°/s（常量可调）；并新增 §7.3 规则定稿、§8 接线双方案 |
| v1.2 | 2026-09-02 | 阶段 1~5 全部完成：交付 MCU/main.c、TankBattle.uvproj（双 Target，基于用户旧工程模板改造并 XML 校验）、PC 六文件 + tools/tests、README.md；STC_BSP.lib 放入可用副本；全量自测通过（逻辑 11 项、串口链路 5 项、像素视觉 10 项、main 冒烟）；剩余阶段 6（真板联调）待用户硬件；记录重要实现决策（字体枚举崩溃→直接探测字体文件路径；loop:// 不适合成对虚拟串口→socketpair 注入） |
| v1.3 | 2026-09-02 | 独立交叉审查两轮（PC/板端 C）并全部修复：PC——KO 后残弹重复结算与幽灵子弹(必修)、坦克互撞不穿模、断线重连解析器残留、无 pyserial 崩溃路径、pygame≥2 版本校验、新增回归测试（逻辑扩至 14 项）；MCU——PLAYER_ID 编译期 #error 校验、Uart1/2Print 成功判据改 !=enumUart1TxFailure；新增验收预演 acceptance_sim.py（验收 1~7 自动对照）；README/开发计划文档同步；全量回归绿（逻辑 14/串口链路 7/验收预演 7/编译 0） |
| v1.4 | 2026-09-02 | 界面质量线上强度：新增并收编两套视觉 QA 交付物——visual_qa.py（六状态画面 11/11 像素断言 + 生成 tests/shots/ 截图供人工预览）与 text_layout_qa.py（HUD 文本无越界/无重叠 + 34 汉字字形无豆腐块，中文乱码可程序化证明）；README/自测证据更新 |
| v1.5 | 2026-09-02 | 用户真机首测反馈三项问题全部修复：① 数码管身份显示——main.c 上电 Seg7Print 最左位显示 1/2（替代默认 00000000，附 LED L0/L7）；② R 键重开容错——game.py 增加 unicode 字符与键盘原始键态双兜底（防中文输入法吞 KEYDOWN），visual_qa 新增 s07 断言；③ Tank_P2.hex 编译路径——新增单 Target 便捷工程 TankBattle_P2.uvproj / TankBattle_P1.uvproj，README 详述 Target 切换/Batch Build/便捷工程三法；全套回归绿（逻辑14/链路7/验收1~7/画面QA 12/布局字形） |
| v1.6 | 2026-09-02 | 状态核验轮：确认 Keil 实编译产出双 HEX 在位、三工程 XML 有效、源码无外部漂移、逻辑14/链路7/验收预演全套仍绿；新增《验收复测清单.md》（修复项 A1~A4 + 验收 1~7 + 汇报模板），等待用户重烧两块板复测 |
| v1.7 | 2026-09-02 | R 键重开深度排查（用户反馈加固后仍无反应）：新增**两条结算重开路径**——① 任一手柄按 K1(开火沿) 即重开（走板子信号不经键盘，最可靠，game.py update 的 game_over 分支实现）；② 回车键(K_RETURN/K_KP_ENTER) 与 R 并列触发（KEYDOWN+原始键态双通道）；结算画面文案同步（R/回车/K1）；main.py 新增 `--debug` 按键诊断开关（打印每次 KEYDOWN 的 key/name/unicode 与重开触发）；回归新增：test_logic 第 15 项"结算画面 K1 重开"、visual_qa s08（回车/手柄K1）；全套绿（逻辑15/链路7/验收1~7/画面QA 13/布局字形） |
| v1.8 | 2026-09-02 | 重开再加固：① 结算画面**鼠标点击任意处**即重开（restart_on_click，鼠标与键盘事件同链路、不依赖键盘/输入法；回归 test_logic 第 16 项 PASS）；② `--debug` 诊断输出追加落盘 `PC\debug_keys.log`（覆盖"双击启动看不到控制台"场景）；README/复测清单同步四种重开方式（R/回车/鼠标点击/任一手柄K1） |
| v1.9 | 2026-09-02 | 按用户最新需求**收敛重开方式为两种**：鼠标点击 与 **任一手柄按 K2**——① 移除 R/回车 键重开（run 事件处理仅剩 Esc/点击；删除原始键态兜底函数）；② 结算重开从 K1(bit4) 改为 **K2(bit5)**：板端 main.c 新增 K2 单发重开请求（GetKeyAct(enumKey2) → restartPending，随包 bit5 发送一次）；PC `_check_restart_from_boards` 按 bit5 上升沿重开，K1(开火)在结算画面不再触发；协议表/文案/FAQ/复测清单同步；验收预演"验收7"改为虚拟板按 K2 真实驱动重开并通过；Keil 重编双 HEX（0 Error，含 bit5）；全套回归绿（逻辑16/链路7/验收1~7/画面QA 12/布局字形） |
| v2.0 | 2026-09-02 | **用户真机验收全部通过（"全过"）**：验收标准 1~7 真机确认（L0/L7+数码管身份、移动转向、K1 开火、命中结算、胜负显示、障碍物、鼠标点击/K2 重开）；README 验收清单全 ☑、开发计划阶段 6 ✅ 完成；项目交付归档。 |
| v2.1 | 2026-09-02 | **多地图与开局平衡改造**（用户反馈：原地图障碍太单一、开局两玩家中间无遮挡可直接对射）：① 修复"开局即对射"——6 张地图全部在中央设置掩体，两出生点正前方直线弹道被遮挡，开局必须走位绕掩体才能交火；② 新增 5 张地图共 **6 张**（经典战场/十字要塞/中央堡垒/回廊/街巷/石柱阵），全部关于 x=400 镜像对称（地形公平）且全图连通（无死区），障碍 4~11 个/图、面积占比 5%~11%；③ `main.py` **每局开局随机抽 1 张**（Game(map_id='random')，结算重开也重抽；map_id=None→默认经典、指定 id→固定复现，左下角显示地图名）；④ 新增 `tests/test_maps.py`（4 项：注册表/每图对称·出生留空·开局直线遮挡·全图连通·规模/选图语义/平衡回归——6 图×双向出生直射实测均被掩体吸收）；回归同步：test_logic 纯物理用例（边界/越界回收/坦克互撞）改开阔场、acceptance_sim 验收3~5 对射场景移到中央掩体右缘空道、visual_qa 直射子弹场景移到下路并新增 6 图渲染冒烟、text_layout_qa 增加左下角地图名文本；全套回归绿（逻辑16/地图4/链路7/验收1~7/画面QA 13/布局字形） |
| v2.2 | 2026-09-03 | **红外无线手柄（新模块，位于 `无线手柄/`）**：新增两套独立 Keil 工程——HEX-A 红外发送端（替代玩家2有线手柄，导航键+K1/K2 每100ms 经 IR(NEC_R05d) 发包 AA 02 xx）与 HEX-B 红外接收端+串口1转发（收包校验后 9600bps 原样转发 PC）；协议与有线完全一致，PC 端零修改；全部新文件放在 `无线手柄/` 内，不触碰 MCU/PC 既有源码与 BSP。**数码管身份区分**：有线玩家1/2 显示 1/2（L0/L7），无线发送端显示 **'A'**（L7）、无线接收端显示 **'b'**（decode_table 追加索引26/27 段码），一眼区分四块板。**HEX 统一输出**：新增根目录 `HEX/`（含 README 对照表），四个工程 Keil Output 均指向该目录——`Wired_P1.hex`/`Wired_P2.hex`/`Wireless_TX_P2.hex`/`Wireless_RX.hex`（原 MCU\output\Tank_P1/2.hex 与无线工程各自 output 作废，目录已清理）；两个无线工程以 C51 Define（WIRELESS_TX/WIRELESS_RX）区分，避免共享输出目录误用旧 obj；本机 UV4 实编译 4 工程 0 Error，Wired_P1/P2 与迁移前 hex MD5 完全一致（确认只搬位置未动代码）。 |
| v2.3 | 2026-09-03 | **随机道具系统（纯 PC 端，协议/板端零改动）**：用户需求"游戏过程中随机出现道具——加速/炮弹增强(命中-2血)/血包(+1命)/护盾(挡1发+限时)"，并逐项确认平衡决策后实现：① 新增 `PC/powerup.py`（PowerUp 类：四种类型常量/拾取半径/几何）；② 新增 `game.py` 道具系统——定时生成（首件开局 5s、之后 8~12s 随机一件、四类等概率、场上同时 ≤2 件、刷点须"整辆坦克可停靠"避开障碍/坦克/道具、10s 无人拾取自动消失且临消失 3s 闪烁）、碾过拾取、四类效果结算（加速 6s×1.5 / 炮强 6s 命中-2 血且伤害于**开火瞬间快照** / 血包 +1 命上限 3 且满血拾取无效并消耗 / 护盾 8s 或挡 1 发先到先失效）；③ 平衡防滚雪球：**阵亡重生清空全部增益与护盾**、护盾挡击不掉血不重生不清其他增益、同类不叠加仅刷新时长；④ 视觉：四种发光图标、增强弹白色大光环、青色护盾环、HUD 增益栏（加速/炮强/护盾+剩余秒数）；⑤ 回归：`test_logic.py` 新增 8 项道具测试（逻辑 16→**24 项**全绿）+ 地图 4/链路 7/画面 QA/文本布局 QA 全绿；README 目录/规则表/道具平衡说明/验证记录与开发计划 §7.3 同步。 |
| v2.4 | 2026-09-03 | **用户三项改进**：① **增益栏倒计时显示恒为 0s 的 bug 修复**——`_draw_buff_bar` 把"剩余时长"当"绝对到期时刻"二次减 now 导致恒 0，改为列表存绝对到期时刻、秒数取 `_buff_seconds(until, now)=ceil(max(0, until-now))`（生效中恒 ≥1s、归零瞬间与效果同一时钟同步失效），抽为静态方法可无头测试；② **血包回血过程醒目**——拾取血包时坦克头顶上浮"生命 +1"（绿）/满血"生命已满"（灰）提示，1.2s 向上飘并渐隐（`Game.toasts` 浮动提示系统，随 update 清理、reset 清空）；③ **开始界面**——`Game(menu=True)` 程序启动先显示开始界面（标题/双方操作说明/道具简介/闪烁"按任意键或点击鼠标开始"），键盘任意键/鼠标点击/任一手柄任意键进入对战，期间场上完全冻结；**结算后 K2/鼠标点击重开直接开下一局不经过开始界面**（`menu_active` 独立于 reset）；main.py 构造改 `menu=True`；回归：test_logic 新增 4 项（28 项总全绿）、visual_qa 新增 s09 开始界面 + s10 开始后场景断言、text_layout_qa 新增 menu 布局审计；README 操作说明/道具反馈/验证记录与开发计划 §7.3 同步。 |
| v2.5 | 2026-09-03 | **道具出现频率适度下调**（用户反馈"略微有点多，适当减少一点点"）：首件延迟 5s→**6s**、生成间隔 8~12s→**10~15s**（随机期望约 10s→12.5s，75s 长时模拟道具生成数约 8→6 件，生成率约降 1/4）；场上同时上限（2 件）、单件寿命（10s）与四类等概率不变；仅改 `game.py` 三个节奏常量（POWERUP_SPAWN_FIRST/MIN/MAX）+ 注释，测试仍全绿（逻辑 28 项等）；README/开发计划 §7.3 数值同步。 |
| v2.6 | 2026-09-03 | **开始界面 K3 可开始 + 菜单文案修正**（用户反馈）：① 协议新增 **bit6=K3**（按下瞬间单发）——此前板端只上报到 bit5（K1/K2/导航），K3 根本没进数据包，故按 K3 无反应；有线 `MCU/main.c` 与无线发送端 `无线手柄/HEX_A_IR_Tx/main.c` 同步增加 K3 轮询/组包/清标志（GetKeyAct(enumKey3)），PC 端无需逻辑改动（开始界面"任意非零掩码"天然覆盖 bit6），`serial_handler.py` 补 `BIT_K3=0x40` 常量与 mask_to_text/文档；**K3 对战中无含义、向后兼容**；Keil 实编译三个工程 0 Error（Wired_P1/Wired_P2/Wireless_TX_P2.hex 已重出）；② 开始界面文案删除"（不再显示本界面）"（game.py `_draw_menu` + text_layout_qa 同步）；回归：test_logic 28 项仍全绿（menu 测试补 K3 位场景），serial_e2e/QA 全绿；README/无线手柄 README/开发计划协议表同步。 |
| v2.7 | 2026-09-04 | **485 三板手柄方案（有线手柄改 485 总线 + 第三块中转板转发 PC）**（用户需求，计划经确认后实施）：两块手柄板不再各用一根 USB 直连 PC，改为两块 485 从机手柄走共享 RS-485 总线，由第三块**中转板**（485 轮询主机 + USB/串口1 原样转发）每 100ms 点名 P1→P2、收包转发 PC；**PC 端协议与代码零改动**（已核实 SerialHub 按玩家 ID 路由、main.py 打开全部候选口 → 单 COM 双玩家现成支持），新增强制回归 `test_serial_e2e.py` 第 8 项"485 中转单 COM：同端口双玩家绑定/整体掉线判离线"。**板间轮询协议（板间私有）**：点名帧 `AA E1 00`/`AA E2 00`（E1/E2=点名码，与玩家号 01/02 永不冲突）、应答帧即原 PC 协议 `AA 01/02 mask`；所有帧固定 3 字节保证总线对齐；中转 30ms/40ms 超时（10ms 计数），单手柄掉线不阻塞另一玩家，PC 0.6s 心跳判"断开!"不变。**交付**：新增 `485手柄\`（main_handle.c 两工程共用 PLAYER_ID 宏、main_hub.c 中转板状态机；三工程共享引用 MCU\inc 与 MCU\STC_BSP.lib，Output 指向 ..\HEX\），三 HEX 入 `HEX\`：**RS485_Handle1.hex / RS485_Handle2.hex / RS485_Hub.hex**（数码管 1/2/C，手柄 L0/L7）；旧 USB 直连（Wired_P1/P2）与红外无线方案保留可选。**验证**：本机 UV4 -b 三工程 0 Error（警告仅 BSP 库 L16）；PC 全量回归绿（逻辑 28/地图 4/链路 8/验收预演 1~7/画面 QA/文本 QA）；README（主推接线 485、目录、FAQ Q10）、HEX\README（7 个 HEX 对照）、开发计划 §3/§5/§8/状态行同步。真机联调（485 接线 + 验收 1~7）待用户按 `485手柄\README.md` 执行（已于 v2.10 复测时完成并通过）。 |
| v2.8 | 2026-09-04 | **手柄 LED 血量联动（新功能，按用户需求确认后实施）**：两块玩家手柄板 LED(L0~L5) 显示**本板所控角色**血量——满血(3)=6 灯；每掉 1 血灭**右侧 2 灯**（2→4 灯、1→2 灯）；死亡=**全灭**；血包回血补亮、K2/鼠标重开复位满血。用户确认三项边界：① 身份=数码管（LED≠数码管，LED 不再亮 L0/L7 身份灯）；② 只在实际操作的玩家板上实现（中转/接收板不显示血量）；③ 上电/未进对局 LED 默认全灭；另确认**红外无线本期不做**、两板统一用 L0~L5。**实现**：① PC→板下行帧 `AA D1 HP`/`AA D2 HP`（HP=0~3；D1/D2 与玩家号 01/02、点名码 E1/E2 永不冲突）；② PC 端 `serial_handler.py`：SerialLink 增加下行发送队列（读写同线程避免并发）、`SerialHub.set_hp()` 组帧下发 + 断线重连/换口自动补发（打开代数+1 触发）；`game.py`：开局(start_game/非菜单直开)、每次命中扣血、血包回血、死亡、K2/鼠标重开 时 `_push_hp`（开始界面期间不推）；③ 板端：`MCU/main.c`（UART_IF=0 用 SetUart1Rxd 收 AA+本板命令码帧 → LedPrint(hpLedTable[hp])，上电全灭）；`485手柄/main_handle.c`（485 接收匹配头放宽为 AA 单字节、回调按码分发：点名→应答、D1/D2→刷新 LED）；`485手柄/main_hub.c`（串口1 收 PC 下行 → 缓存 → 总线空闲窗口 tickCnt≥2 每 10ms 至多一帧转发给对应手柄）；无线/HEX-A、HEX-B 未改动。**验证**：PC 全量回归绿——逻辑 28→**30 项**（新增 LED 下行推送时机 2 项：开局/命中/回血/死亡/重开推送、菜单不推·开局才推满血）、链路 8→**12 项**（新增下行帧按玩家送达/重连自动补发/集成命中扣血下行/485 单 COM 双下行）、验收预演新增**验收 8**（命中→AA D2 02、死亡→AA D2 00、K2 重开→两板 AA D1/D2 03）全过、画面 QA/文本 QA 绿；五份固件（Wired_P1/P2、RS485_Handle1/2/Hub）本机 UV4 -b **0 Error**（警告仅 BSP 库 L16），P1/P2、Handle1/Handle2 HEX MD5 不同（身份命令码正确区分）；README/HEX README/485 README/验收复测清单/开发计划 §1~§8 同步（LED=血量、身份=数码管新语义，验收 1 判据更新、新增验收 8）。**真机复测（2026-09-04 用户确认）：验收 8 在 USB 直连与 485 三板两套接法下全部通过。** |
| v2.9 | 2026-09-04 | **HEX 统一重命名（用户需求：文件名一眼分辨用途）**：改为「连接方式前缀 + 角色」命名——485 方案：RS485_Handle1→**485_Player1.hex**、RS485_Handle2→**485_Player2.hex**、RS485_Hub→**485_Relay.hex**；旧 USB 直连：Wired_P1→**USB_Player1.hex**、Wired_P2→**USB_Player2.hex**；红外无线：Wireless_TX_P2→**IR_Player2.hex**（发送端/玩家2 无线手柄）、Wireless_RX→**IR_Relay.hex**（接收端转发）。**实施**：8 个 uvproj OutputName 同步（485 三工程、MCU 双 Target+两单 Target、无线两工程），全部 hex 从当前源码 UV4 重编 0 Error 且与更名前逐字节一致（MD5 不变，证明纯更名、未动代码）；HEX 目录旧名 hex 与中间产物清理，.gitignore 改通用规则（HEX/* 忽略中间物、保留 *.hex 与 README）；工程文件名（RS485_Handle1/2、RS485_Hub、TankBattle_*、HEX_A/HEX_B.uvproj）保持不变、仅 OutputName=hex 名；README/HEX README/485 README/无线手柄 README×3/验收复测清单 引用同步。数码管身份速查：1/2（玩家手柄，485 或 USB 接法）、C（485 中转）、A/b（无线收发）；玩家手柄 LED 为血量条（v2.8），与身份解耦。 |
| v2.10 | 2026-09-04 | **用户真机复测通过（验收 1~8 归档）**：按《验收复测清单.md》A1~A7 与验收 1~8，在 **USB 直连两板（USB_Player1/2.hex）与 485 三板（485_Player1/2 + 485_Relay.hex，v2.9 新名）两套接法**下逐项真机验证，全部 PASS——① 数码管身份 1/2、LED 上电默认全灭；② 开局满血两块板 L0~L5 六灯全亮；③ 命中灭右侧 2 灯、再命中再灭、另一玩家板互不影响；④ 血包回血补亮 2 灯；⑤ 生命归零 LED 全灭、鼠标/K2 重开回 6 灯；⑥ 拔板重插 LED 按当前血量自动恢复；移动/开火/命中结算/胜负/障碍/重开/断线提示亦复测通过。README 验收清单 1~8 全 ☑、开发计划状态行/风险 5/验证记录同步。 |
| v2.11 | 2026-09-05 | **蜂鸣器音效（玩家手边板，测试开发；真机试听通过 ✅）**：给玩家手边的板子（有线 USB 手柄×2、无线发送端 HEX-A、可选 485 手柄×2）加蜂鸣器音效反馈，电脑旁板（无线接收端 HEX-B、485 中转板）不加。**新模块 `sound\sound_effect.h/.c`**：对外 `Sound_Init / Sound_Play / Sound_IsBusy / Sound_Tick`；宏 `SOUND_ENABLED`（0=全空操作、行为与原版一致）、`SOUND_VOLUME=3`、`SOUND_PRIORITY_ENABLE=1`；10ms 状态机支持单音与序列（双短促/下扫）、同刻单音、高优先级抢占（BSP 限制：正在响的单音无法 API 截停，抢占=丢剩余音步+空闲即接播）；音效定义：开火 1500Hz/50ms(高)、移动 800Hz/20ms(低，方向按住每100ms)、被击中 500Hz/200ms(中)、击中 1200Hz×80ms×2(中，预留)、结束 1500→500Hz 各100ms(高)。**音量**：`SetBeep` 无占空比接口且约束禁直写寄存器 → 串联 100Ω~1kΩ 限流电阻（SOUND_VOLUME=3 ↔ 470Ω~1kΩ 起步，20cm 可听）。**集成（侵入最小，改动点带【音效】标注）**：main() 加 `Sound_Init()`；注册 10ms 回调驱动 `Sound_Tick()`；K1→FIRE、方向按住→MOVE、PC 下行血量帧 `AA D1/D2 HP` 下降沿→DAMAGE、归零→GAMEOVER（不新增协议、防重复响）。**工程现状**：MCU\、485手柄\、无线手柄\HEX_A_IR_Tx\ 复制并挂载 sound_effect.c（HEX-A inc 补 Beep.h）；HEX-B 只放头文件（不挂 .c、SOUND_ENABLED=0）；485 中转板 main_hub.c 未动。**验证**：本机 UV4 -b 六工程 0 Error（USB_P1/P2、485_P1/P2、IR_TX、IR_RX；警告仅 BSP 库 L16）；模块单独编（ENABLED=1/0）均 0 Error 0 Warning；HEX\USB_Player1/2、485_Player1/2、IR_Player2 已重出（IR_Relay 二进制与原版一致、485_Relay 未重编）。真机试听（K1 哒 / 方向嗒嗒 / 20cm 音量 / 命中与结束音 / 接收端无声 / 功能回归）已于 **2026-09-05 用户真机确认通过**。 |


---

## 附：蓝牙无线方案定稿记录（2026-09-07 追加）

- 蓝牙手柄为第④接法（第四套板端方案），目录 `01_下位机程序/05_蓝牙模式/`；
- **正式路线（定稿）**：BT05（CC2541 BLE 透传）+ 板 EXT 口 UART2 全双工，
  PC 端 `tools/ble_bridge.py`（bleak）+ VSPD 虚拟串口对 COM20/21，游戏零改动
  （`--p1 COM20`）；HC-05/HC-06 经典蓝牙仅保留为备选说明。
- 真机联调通过（2026-09-06 用户确认：上行 100ms/帧、下行 LED、游戏完整游玩）。
- 日常使用（定稿，2026-09-07 用户实测通过）：蓝牙板上电 → 双击根目录
  `启动BLE桥接.bat`（单窗口桥接）→ 双击 `启动游戏.bat`（`python main.py --p1 COM20`）。
- 后续开发方向（如做）：双蓝牙双玩家（第二模块 + 第二 VSPD 对 + 第二桥接实例）、
  把 VSPD/桥接纳入自动初始化脚本。
