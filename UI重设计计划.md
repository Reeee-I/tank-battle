# 双人坦克对战 · UI 重设计计划（纯表现层重构 · 定稿 v2）

> 状态：**计划已定稿，待你确认开工**（尚未改任何游戏代码）
> 目标：用 `picture/` 素材把 PC 端画面整体换新。**不改变任何功能与机制**。
> 边界：窗口保持 800×600；逻辑坐标 800×600 不变；MCU/串口/协议/键盘/规则常量/地图不动；
> 不新增 pygame 之外的依赖；`picture/` 原始素材只读不写。
> 更新：基于"现在能读图"重新审视，修正了 v1 中凭判断/猜测的部分，并纳入你的 6 项决策。

---

## 〇、素材实际画面（已逐张查看，作为设计依据）

| 文件（picture/，无扩展名） | 实际内容 |
|---|---|
| 玩家1坦克素材 | **蓝色像素风坦克，侧/斜视画法**：炮管朝右、履带+负重轮在下、中央炮塔带圆形舱盖、细天线 → 基图 `tank_p1.png`，0°=朝右(+x) |
| 玩家2坦克素材 | **与玩家1文件完全相同（也是蓝色）** → 占位 `tank_p2.png`；**你稍后提供红色版**，到位后替换 |
| 子弹素材 | **黄色发光彗尾弹**：大头在右、焰尾在左（有速度/方向感的弹体）→ `bullet.png`，炮口朝向+右为 0° |
| 障碍物素材 | **深灰金属方板**，底部一条黄黑警示条+底座，四角铆钉 → `obstacle.png`（单块方块，用于平铺） |
| 整体示意图 | 只作**结构与风格参考**（用户确认：布局由助手自行安排）。它示意：深绿网格战场、深色金属边框+橙色标题、左右玩家面板、黄彗尾弹、金属方板障碍、底部 TIME/旗帜/FPS |

> 示意图是通用模板，其中"血条 85/100"与"倒计时 TIME"游戏并不存在，
> 按你的决策：**不引入**，HUD 全部换算成真实数据（见第 2.3 节）。

---

## 一、视觉方向（定稿）

- **风格**：暗色军事机甲风，延续示意图气质——深橄榄绿网格战场地面、深灰金属边框与面板、橙色标题、像素风坦克/子弹/障碍。全局用 `convert_alpha()`，60FPS 无压力。
- **构图**：沿用"全屏 800×600 = 战场区"，HUD 以**覆盖层**浮在边缘（不缩内场、不改坐标，机制零改动）。金属边框/顶部标题板/底部信息栏为装饰性覆盖层；坦克出生点 y=300、障碍基本居中，HUD 只在边缘与之轻微重叠，可接受。

```
┌──────────────────────── 800×600 ────────────────────────┐
│▌金属边框 ▌ [P1面板]   ▓ TANK BATTLE ▓   [P2面板] ▐金属边框▐│ ← 顶部覆盖层
│            ●在线 ❤❤❤ 得分            ●在线   ❤❤❤ 得分   │
│                                                       │
│            ┌────── 战场(整屏逻辑坐标不变)──────┐        │
│            │  绿网格地面 + 障碍(平铺) + 坦克 + 子弹  │        │
│            └──────────────────────────────────┘        │
│   [旗帜2]  地图·xx     操作提示(底中)    得分/旗帜1  FPS │ ← 底部覆盖层
└───────────────────────────────────────────────────────┘
```

---

## 二、分阶段实施步骤

### 阶段 0：素材入工程 + 加载模块
1. 建 `PC/assets/`，拷贝并命名：`tank_p1.png`、`tank_p2.png`（占位，待红色版）、
   `bullet.png`、`obstacle.png`、`bg.png`（背景占位，待你的地面素材）；
2. 写 `PC/assets.py`：懒加载 + 缓存 + `convert_alpha()`；缩放/旋转缓存；
   **缺素材自动回退现有矢量画法**（换机/缺文件也能跑，功能不塌）。

### 阶段 1：战场四要素（game.py 渲染层）
3. **背景**：绿金属网格地面（先代码生成无缝绿色网格占位；你的地面素材到位后替换 `bg.png`）；
4. **障碍**：按你的决策**按方块平铺**——把 `obstacle.png` 按固定块尺寸（约等于网格 40px）
   平铺填满每个障碍矩形，超出部分裁剪；改进项：仅**最底一排**块显示黄黑警示条+底座，
   其余块只保留金属面，以贴近示意图"成排金属板、最底带警示条"的观感（实施时用截图核对）；
5. **坦克**：`tank_p1` 基图 0° 朝右；`tank_p2` 先用**蓝→红换色**生成占位，红图到位即替换。
   绘制：取图 → 缩放使车体≈30px、旋转角=坦克角度、**旋转中心=炮塔/车身中心**（不是图中心，
   因为素材炮管偏右，需先算出图中的转轴并平移到 (x,y)）；无敌白圈保留在上层；
6. **子弹**：`bullet.png` 黄彗尾弹，按子弹飞行方向旋转（0°=朝右），缩到约 14~18px；
   归属区分不强制（示意图两发都是黄的），如需保留区分可加轻微边色（可选项）。

### 阶段 2：HUD 与浮层重设计（换算成真实数据）
7. 顶部标题板（金属底 + 橙色大号 **TANK BATTLE** 居中）+ 左右玩家面板：
   - 面板：金属框 + 坦克小头像（裁坦克图）+ 玩家标签（**中英双语**："PLAYER 1 / 玩家1"、
     "PLAYER 2 / 玩家2"）+ 连接状态点（绿/橙/灰）+ **3 颗心（生命）** + 得分；
8. 底部信息栏：左=旗帜+得分、中=操作提示、右=地图名 + FPS（文字内容与现状一致，仅排版/面板重绘）；
9. **等待连接** / **断线提示**：保留语义，面板换成金属风格；
10. **胜负结算**：金属面板 + 标题"玩家X 获胜！"+ 比分 + 重开提示（点击鼠标 / 任一手柄 K2）+ Esc 退出；
    重开方式与触发逻辑不变。

### 阶段 3：回归验证
11. 逻辑零改动证明：`test_logic.py`/`test_maps.py`/`test_serial_e2e.py`/`acceptance_sim.py`
    不改期望必须全绿；
12. 更新 `tests/visual_qa.py`、`tests/text_layout_qa.py` 的坐标与颜色断言到新画面（断言含义保留：
    P1 坦克左出生区、子弹在弹道区、中央障碍存在、红心灰心、橙点断线、胜利大字、全地图渲染）；
13. 真机冒烟：`python main.py`（无板子、键盘双人）跑 60FPS，确认无素材加载错误、FPS 不降；
    临时移走 assets 验证回退路径可运行（自查项）。

### 阶段 4：交付
14. 差异自查：仅渲染层 + 新增 assets 模块 + 两个测试文件 + 文档变更；
15. 更新 `README.md` 与 `开发计划.md`（素材目录、画面 QA、本次变更日志）；
16. 对照 README 验收清单 1~7 人工复跑（机制未动，应照旧全过）。

---

## 三、关键技术点（正确性核心）

| 元素 | 逻辑事实 | 贴图细节 |
|---|---|---|
| 坦克转轴 | 坦克绕中心 (x,y) 旋转，炮口=中心外 23px | 素材炮管偏右，旋转轴≈炮塔中心（需按图测量），缩放后平移对齐到 (x,y)；炮口视觉终点≈23px，若有偏差做"炮口对齐校准" |
| 障碍平铺 | 各矩形 w×h（44~360px） | 固定块尺寸平铺+裁剪；仅底排版显示警示条 |
| 玩家颜色 | P1 蓝 / P2 红 | 蓝色基图 + 蓝→红换色生成红色占位；红图到位直接替换 `tank_p2.png` |
| 性能 | — | 障碍每 (w,h) 缓存；坦克按 (玩家,整数角度) 缓存旋转；子弹贴图极小 |

---

## 四、待你补充

1. **玩家2红色坦克图**（到位后替换占位；缺省先用蓝→红换色）
2. **背景地面素材**（到位后替换 `bg.png`；缺省先用代码生成的绿色网格占位）

---

## 五、下一步

**按你的决定：暂不动手，等你提供两份素材后开工。**
- 等待补充：① 玩家2红色坦克图；② 背景地面素材。两者到位后我替换 `tank_p2.png` / `bg.png`，
  即从**阶段 0**（素材入工程）开始按上文顺序实施，并逐屏出截图供你确认。
- 标题最终定稿：橙色大号 **TANK BATTLE**（英文），玩家标签中英双语
  （"PLAYER 1 / 玩家1"、"PLAYER 2 / 玩家2"）。

---

*本文件为规划文档，不构成任何游戏代码修改；执行以你的确认为准。*
